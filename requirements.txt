anthropic>=0.100.0
playwright>=1.48.0
openpyxl>=3.1.5
python-dotenv>=1.0.1
pdfplumber>=0.11.0
pywin32>=306; sys_platform == "win32"
# Optional: nicer Windows toast for the live watcher (watch.py). If absent, the
# watcher falls back to a PowerShell tray balloon, so this isn't required.
winotify>=1.1.0; sys_platform == "win32"
