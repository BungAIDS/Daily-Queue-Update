# Secrets and session — never commit these
.env
cbc_session.json

# Output
output/
snapshots/
*.xlsx
cron.log

# Watcher logs (rotated daily, kept ~a week) — runtime only, don't commit
logs/
watch.log*

# Python
venv/
__pycache__/
*.pyc
